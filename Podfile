use_frameworks!

abstract_target 'Common' do
  pod 'DateUtilities', :path => '.'
  pod 'Nimble', '~> 7.0'

  target 'iOSTests' do
    platform :ios, '8.0'
  end

  target 'tvOSTests' do
    platform :tvos, '9.0'
  end

  target 'macOSTests' do
    platform :osx, '10.10'
  end
end
